namespace BasicGameFrameworkLibrary.GameGraphicsCP.Interfaces
{
    public interface IProportionBoard
    {
        float Proportion { get; } //same as the other but we want to make it clear its for a gameboard.
    }
}