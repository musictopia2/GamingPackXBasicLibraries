namespace BasicGameFrameworkLibrary.MultiplayerClasses.EventModels
{
    /// <summary>
    /// i think that if this is called, then the base shell view model will load the main screen.
    /// </summary>
    public class LoadMainScreenEventModel
    {
    }
}
