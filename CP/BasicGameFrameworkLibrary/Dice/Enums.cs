namespace BasicGameFrameworkLibrary.Dice
{
    public enum EnumStyle //this is still needed so the draw routines can draw what is needed
    {
        Regular, DrawWhiteNumber //this is still needed so it can draw white numbers. its needed for drawings.
    }
}